# Typed i18n for hard-coded literals

`ConnectScreen` still shows the product name "Macro Grid" and a placeholder host address as literals, and `SettingsPanel` maps orientation values to keys by hand. Moving these into the i18n dictionaries is a small change but touches user-visible text, so it is proposed rather than done.
